import { writable, type Writable } from 'svelte/store';

const { subscribe, update, set }: Writable<AntibiogramTitle> = writable({
    Antibiograms
});

const loadAntibiograms = (antibiograms)=>{
    for 
}

export const Antibiograms = {subscribe}